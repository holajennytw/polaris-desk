# 全組工作進度紀錄（2026-06-22）

> 更新時間：2026-06-22 傍晚｜撰寫：R7

---

## R7 今日完成

### PR #4（已 merged）包含
- `block-env-read.sh`：攔截 Read/Bash/Grep 讀取 .env；`.gitattributes` 鎖 LF
- `redaction.py`：補 TAVILY_API_KEY pattern
- `OnboardingModal.tsx`：signIn 補 `callbackUrl: "/research"` — 修登入後無 AppShell
- `ResearchTour.tsx` + `research/page.tsx`：新手引導完全離線化（mock 資料 + 假動畫）
- `notifications/page.tsx`：訂閱儲存失敗補紅字提示
- `peer/page.tsx`：引用 chunk 改呼叫 `api.chunk(sourceId)`

### 本次 commit（準備中）新增
- **Loading 動畫**：研究助理 + 同業比較的 dock-send 旋轉、思考追蹤三點動畫、5s 切換提示文字
- **contraAlertStore page 隔離**：修研究助理 / 同業比較警示互相污染的 bug
- **同業比較引用追蹤器**：移除空白 CitationList，改顯示待後端說明
- **TypeScript 修復**：notifications 頁 useContraAlerts build error

---

## Issue 狀態總覽

| Issue | 標題 | 狀態 | 前端行動 |
|-------|------|------|---------|
| #3 | R3 待交端點 × 7 | 🔴 OPEN | 前端已就緒，等後端 |
| #5 | block-env-read.sh fails open | 🟠 PR #9 OPEN | 本地版本已修（sed/grep），需注意 PR #9 merge 衝突 |
| #6 | /ask bm25 stub | 🟠 PR #10 OPEN | 純後端，PR #10 merge 後前端更新 citation adapter |

---

## PR #4 Test Plan 驗證結果

| 測試項目 | 狀態 | 說明 |
|---------|------|------|
| `POST /research` BQ 資料 + evidence[] | ⚠️ | PR #10 (OPEN) 正在修 citation metadata |
| `GET /notifications` 已讀/未讀切換 | ✅ | 正常 |
| `GET/POST /subscriptions` Firestore | ❌ | Cloud Run 缺 `GOOGLE_CLIENT_ID` → 401 |
| `DELETE /history/{id}` + fallback | ❌ | 後端無此路由；原 PR #138 GitHub 查無 |
| 監控警示面板 5 筆 Watchdog | ⚠️ | endpoint ✅ 但 AlertResponse 缺 origin/title/source/time |
| 同業比較示範資料 banner | ✅ | 正常顯示 |

---

## 各角色待辦（R7 提出，需跨角色協作）

### 🔴 R1/R2 — 部署緊急

| 項目 | 影響 | 指令 |
|------|------|------|
| Cloud Run 補 `GOOGLE_CLIENT_ID` | **訂閱 / 歷史所有需登入端點全 401** | `gcloud run services update polaris-api --region asia-east1 --set-env-vars GOOGLE_CLIENT_ID=14326813937-rkr1h4u1akpllkk62l8ui23sqqiau734.apps.googleusercontent.com` |
| `DELETE /history/{session_id}` 路由 | 對話紀錄刪除功能 | 加後端路由（或確認誰負責） |

### 🔴 R3 — 高優先端點（Issue #3）

| 端點 | 影響功能 | 規格 |
|------|---------|------|
| `GET /alerts` 補 `origin`/`title`/`source`/`time` 欄位 | 監控警示面板正確分頁顯示 | R3_需求清單 §1 |
| `POST /contradiction` | 矛盾偵測改真實 LLM | R3_需求清單 §2 |
| `GET /chunk/{source_id}` | 引用展開顯示完整段落 | R3_需求清單 §4（#115） |

### 🟡 R3 — 中優先端點（Issue #3）

| 端點 | 影響功能 | 規格 |
|------|---------|------|
| `POST /research` 補 `kpis`/`chart` | 量化分析 KPI 卡 + 毛利率趨勢圖 | R3_需求清單 §9 |
| `GET /tracking-feed` | 追蹤通知 tab | R3_需求清單 §8 |
| `GET /suggestions?mode=peer` | 同業比較快速 chip 動態化 | R3_需求清單 §5 |
| `POST /peer-compare` | 同業比較真實資料 | R3_需求清單 §6 |

### 🟡 R4 — 待交端點

| 端點 | 影響功能 | 規格 |
|------|---------|------|
| `GET /library` | 資料庫頁面完全空白（無資料） | R4_需求清單 §1（UI 已就緒，只缺後端） |

### 🟡 R3 — PR 待 merge

| PR | 內容 | 前端 merge 後動作 |
|----|------|-----------------|
| PR #10 (OPEN) | Citation metadata 補 `event_key`/`source_key`/`published_yyyymm` | 更新 `adapters.ts` `normalizeResearch()` citation 對應欄位 |
| PR #9 (OPEN) | block-env-read.sh 安全修正 | 確認不覆蓋本地 sed/grep 版本 |

### 🟡 Jenny (R4) — 私訊

| 項目 | 用途 |
|------|------|
| `GOOGLE_CLIENT_SECRET` | 本地 Google Login 測試（.env.local 缺此值） |

---

## 本地環境狀態（R7）

- 分支：`frontend_2026_0620`
- 未 commit：12 個檔案（今日所有修改）
- dev server：`localhost:3000` ✅
- TypeScript：✅ 無錯誤（`npx tsc --noEmit` 通過）
- `NEXT_PUBLIC_API_BASE`：Cloud Run ✅
- `GOOGLE_CLIENT_ID`：`.env.local` 已設 ✅
- `GOOGLE_CLIENT_SECRET`：⏳ 等 Jenny

---

## 密鑰風險掃描（commit 前確認）

- ✅ staged 檔案無真實金鑰（`tvly-`、`sk-`、`AIzaSy`、`ya29.` 等 pattern 均未出現）
- ✅ 文件中提到的金鑰名稱均為說明文字，非真實值
- ✅ `.env.local` 不在 staged 清單中
- ✅ `block-env-read.sh` 本身不含任何金鑰
