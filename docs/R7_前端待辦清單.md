# R7 前端待辦清單

> 整理日期：2026-06-17
> 依賴狀態：✅ 可動工｜⏳ 等 R3 交付｜🔴 阻塞中

---

## 研究助理頁（/research）

### 可動工

| 項目 | 說明 | 狀態 |
|------|------|------|
| 「送同業比較」按鈕 | 點擊後帶著當前 query 跳轉 `/peer?q=...`，讓同業比較頁自動填入公司 | ✅ |
| 快速提問 chip（動態） | 已串 `GET /suggestions`，BQ 有資料後自動更新；無資料時走 fallback 靜態 chip | ✅ |
| 完整報告匯出 PDF | `ReportModal` 已實作 | ✅ 已完成 |
| 重新分析按鈕 | 帶相同 query 重新呼叫 `POST /research` | ✅ 已完成 |
| 監控警示 → 寫入通知 | 每當監控面板收到新警示（research + contradiction），呼叫 `POST /notifications/events` 記錄 | ✅ |

### 等 R3 交付

| 項目 | 等待內容 | 狀態 |
|------|----------|------|
| 監控系統警示面板顯示真實資料 | 等 `GET /alerts` 補 `origin` 欄位 | ⏳ |
| 矛盾偵測 → 真實後端 | 等 `POST /contradiction` 端點；交付後移除 client mock | ⏳ |
| 引用追蹤器文件標籤 | 等 PR #6 merge，`doc_type`/`published_at`/`fiscal_period` 正確帶入 | ⏳ |
| 引用追蹤器點開原文 | 等 `GET /chunk/{source_id}` 端點；交付後 DocViewer 顯示 BQ 真實段落與頁碼 | ⏳ |
| 摘要內文顯示真實分析 | 等 BQ 法說稿入庫（R4）+ R3 retriever 運作後，`/research` 回傳的 `final_answer` 才有實質內容 | ⏳ |
| ReAct OBS 顯示真實觀察 | 同上，`react_steps` 內容來自後端 LLM 思考過程 | ⏳ |

---

## 同業比較頁（/peer）

### 可動工

| 項目 | 說明 | 狀態 |
|------|------|------|
| 快速提問 chip 改動態 | 目前靜態 hardcoded，改串 `GET /suggestions`（與研究助理同一端點） | ✅ |
| peer-toolbar 公司中文名 | 組員正在處理（目前只顯示 ticker），完成後前端自動更新 | 🔴 等組員 |
| 監控警示 → 寫入通知 | 與研究助理相同邏輯，`origin="peer"` 警示出現時 POST /notifications/events | ✅ |
| 比較報告匯出 PDF | 同業比較結果匯出為 PDF（與研究助理頁 `ReportModal` 相同元件，帶入比較頁內容） | ✅ |

### 等 R3 交付

| 項目 | 等待內容 | 狀態 |
|------|----------|------|
| 整頁從 mock 換成真實資料 | 等 `POST /peer-compare` 端點；交付後替換 hardcoded mock | ⏳ |
| ReAct trace 顯示真實步驟 | 等 `/peer-compare` 回傳真實 `react_steps` | ⏳ |
| 引用追蹤器顯示真實引用 | 目前 `citations=[]` hardcoded，等後端回傳 | ⏳ |
| 監控警示顯示真實資料 | 等 `GET /alerts` 補 `origin="peer"` 欄位 | ⏳ |
| 矛盾偵測 → 真實後端 | 與研究助理共用同一 `/contradiction` 端點 | ⏳ |

---

## 通知中心（/notifications）

### 可動工

| 項目 | 說明 | 狀態 |
|------|------|------|
| 「規則管理」改為「訂閱設定」 | 命名邏輯不符實際功能 | ✅ |
| 訂閱設定 UI | 讓使用者**主動選擇要追蹤的公司**（multi-select，從 `/companies` 取得清單）；選完呼叫 `POST /subscriptions` 儲存 | ⏳ 等 R3 實作 POST /subscriptions |
| 追蹤通知顯示邏輯 | 訂閱後，「追蹤通知」tab 顯示該公司最新入庫的文件或新聞類別提示（doc_type / news）| ⏳ 等 R3 實作 GET /subscriptions |
| 風險動態 feed | 已整合 `/alerts` + `contraAlertStore`，等 `origin` 欄位補好後即可顯示 | ⏳ R3 |

---

## 新聞頁（/news）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 串接 `events` 資料表 | 目前後端 `/news` 查 `chunks WHERE doc_type='news'`，應改查 R4 的 `events` 表 | 🔴 等 R4 確認 schema |
| 標題可點擊連結 | 點擊標題跳轉外部 URL（`target="_blank"`），已完成 UI | ✅ 已完成 |
| 顯示邏輯 | 只顯示標題 + 連結，不顯示內文（新聞最多保留一個月） | ✅ 已完成 |

---

## 資料庫頁（/library）

| 項目 | 說明 | 狀態 |
|------|------|------|
| ticker 篩選標籤 | 加入 ticker 維度的分類 tab，顯示中文公司名（從 `/companies` 取得） | ✅ |
| doc_type 中文標籤 | 現有 `types` tab 已有部分；確認後端回傳的 `types` 含所有 doc_type | ⏳ 等 R4 入庫 |
| 資料來自真實 BQ | 目前 `/library` 端點待實作；等 R4 入庫後接真實資料 | 🔴 等 R4 |

---

## 對話紀錄頁（/history）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 紀錄寫入 | 使用者每次在研究助理 / 同業比較送出查詢後，呼叫 `POST /history` 寫入後端 | ⏳ 等 R3 實作 POST /history |
| 點擊跳轉 | 點擊紀錄跳轉回對應頁面並帶入 query（已完成） | ✅ 已完成 |

---

## 設定頁（/settings）

| 項目 | 說明 | 狀態 |
|------|------|------|
| Google OAuth 真實串接 | 目前按鈕 UI 存在但無實際認證功能，需串接 Google OAuth | ✅ 可動工 |
| 工作郵箱 Magic Link | 前端表單已做，需後端 `POST /auth/email` 端點配合 | 🔴 確認後端 |

---

## 共用邏輯（研究助理 + 同業比較共同維護）

| 項目 | 說明 |
|------|------|
| **矛盾偵測** | 兩頁面各自有獨立的 `runContradictionCheck()`，觸發同一個 `/contradiction` 端點，結果存在各自頁面的 `contraAlertStore`（以 sessionStorage 隔離，不互相干擾） |
| **監控警示面板** | 兩頁各自過濾 `origin`（research / peer），`contradiction` 兩頁都顯示 |
| **引用追蹤器** | 共用 `CitationList` + `DocViewer` 元件，點擊展開從 `/chunk/{source_id}` 載入原文 |
| **警示 → 通知記錄** | 兩頁面各自在警示出現時呼叫 `POST /notifications/events`，通知中心「風險動態」tab 匯總顯示 |

---

## 優先順序建議

```
本週可動工（不等後端）：
  1. 「送同業比較」按鈕接線
  2. 同業比較頁 chip 改動態
  3. 通知中心「規則管理」→「訂閱設定」+ 訂閱 UI
  4. 資料庫頁加 ticker 篩選
  5. 監控警示 → POST /notifications/events 記錄

等 R3 PR #6 merge：
  6. 測試引用追蹤器文件標籤

等 R3 交付 /alerts origin 欄位：
  7. 兩頁監控面板顯示真實警示

等 R3 交付 /contradiction：
  8. 移除兩頁的 client mock，改真實呼叫

等 R3 交付 POST /history：
  9. 對話紀錄頁寫入串接（研究助理 + 同業比較查詢後自動呼叫）

等 R3 交付 POST /subscriptions + GET /subscriptions：
  10. 訂閱設定「儲存」按鈕真實寫入
  11. 追蹤通知 tab 依訂閱公司顯示最新入庫提示
```
