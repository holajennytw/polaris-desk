# R7 工作紀錄 — 2026-06-24

> 撰寫：R7（angelali2026888）
> 狀態：當日結束存檔

---

## 一、今日已完成

### 1. 後端 — event_key / source_key 欄位對齊（R6 schema 更名同步）

R6 於 BQ 將 `events` 表欄位更名：`event_type → event_key`、`source_name → source_key`。
R7 同步更新後端：

| 檔案 | 改動 |
|------|------|
| `src/polaris/structured_store.py` | `list_events()` SQL 改用 `event_key`、`source_key`；`STARTS_WITH(event_key, @event_key)` 取代 exact match（支援 `major_news.others` 等子類型） |
| `src/polaris/api.py` | `EventResponse` model：`event_type→event_key`、新增 `source_key` |

### 2. 後端 — `GET /library` 端點實作（R7 自行完成）

原本開 issue #20 請 R4 實作，評估後 R7 直接用 `structured_store.py` 完成：

| 資料類型 | 來源 | 狀態 |
|---------|------|------|
| `transcript`（法說逐字稿） | `chunks` `doc_type='transcript'` GROUP BY ticker+fiscal_period | ✅ |
| `major_news`（重大消息） | `events` `STARTS_WITH(event_key,'major_news')` | ✅ |
| `earnings_call`（法說簡報） | 等 R4 vision-OCR ingestion | ⏳ #22 已關，改等 chunks `doc_type=presentation` |

**注意**：`chunks.chunk_text` 不在查詢範圍（有 ACL），只查文件級 metadata。

### 3. 前端 — 量化分析 panel 隱藏（R4 建議延後）

`research/page.tsx`：移除量化分析區塊，改留 comment 標記接線位置。

### 4. 前端 — 引用 chip 顯示修正

**問題**：摘要條列的引用 chip 顯示 `s.cite==="fin"?"財報":"法說"` 對真實 `mops_...` source_id 一律顯示「法說」。

**修正**：

| 檔案 | 改動 |
|------|------|
| `frontend/src/types/viewmodel.ts` | `SummaryItemVM` 新增 `doc_type_label?: string` |
| `frontend/src/lib/adapters.ts` | `normalizeResearch()` 從 `evidence[i]` 取 `citationLabel()` 填入 `doc_type_label` |
| `frontend/src/app/(dashboard)/research/page.tsx` | chip 改用 `{s.doc_type_label ?? "文件"}`；無 cite 時不渲染 chip |

### 5. 前端 — 財務 KPI 卡片補引用（cite）

**問題**：`financialsToKpis()` 回傳的 KPI 沒有 `cite` 欄位，KPI 卡片點擊沒反應。

**修正**：

| 檔案 | 改動 |
|------|------|
| `frontend/src/hooks/useFinancials.ts` | `financialsToKpis()` 每張卡補 `cite: row.source_id ?? ""` |
| `frontend/src/app/(dashboard)/research/page.tsx` | 財務 KPI 改用 `<KpiCard k={k} onCite={handleOpenDoc}/>` |

### 6. 前端 — 同業比較季度選單動態化

**問題**：`PERIOD_OPTIONS` 硬編碼 6 個季度，不反映資料庫實際有的季度。

**修正**：

| 檔案 | 改動 |
|------|------|
| `frontend/src/app/(dashboard)/peer/page.tsx` | 在 page level 加 `useFinancials(aId)` + `useFinancials(bId)`；`availablePeriods` 從兩公司的 `fiscal_period` 取聯集、排序後填入 `<select>`；query 解析出的 period 也改用 `availablePeriods.includes()` 驗證 |

### 7. Issue 管理

| 動作 | Issue | 說明 |
|------|-------|------|
| 關閉 | #3 [R3] 前端待接端點彙總 | 內容整合進 #23，留 comment 指向新 issue |
| 開立 | #23 [R3/R4] 研究助理 + 同業比較：全資料來源 RAG + 端點規格 | assign officehsieh-afk + holajennytw |
| 留言 + 關閉 | #22 [R4] colpali_pages BQ error | ColPali gate ③ FAIL，colpali_pages 不再使用；earnings_call 改等 vision-OCR ingestion |
| 留言 + 關閉 | #17 ColPali 閘③ | gate ③ hit@5=0%（embedding collapse），單向量第 4 路收掉，改走 vision-OCR-to-text |

---

## 二、未完成 / 待辦事項（R7 自己可做）

| 優先 | 項目 | 備注 |
|------|------|------|
| 🟡 | Commit 本次所有改動 | 含後端 event_key 對齊、/library 端點、前端 3 項修正、舊文件刪除 |
| ⚪ | `normalizeResearch()` 解封 kpis/chart | 等 R3 `POST /research` 補欄位後，`adapters.ts` 一行改動 |
| ⚪ | `GET /alerts` 加 origin filter | 等 R3 補 origin 欄位後，`research/page.tsx` + `peer/page.tsx` 各加 filter |

---

## 三、等待他人的阻擋項

### 等 R3 + R4（issue #23）

| 優先 | 項目 | 影響 |
|------|------|------|
| 🔴 | `POST /research` 跨 doc_type RAG | 研究助理無法分析多種資料來源 |
| 🔴 | `GET /chunk/{source_id}` | 引用點擊只能看 snippet |
| 🔴 | `POST /peer-compare` | 同業比較整頁 hardcoded mock |
| 🟡 | `GET /alerts` 補 `origin` | 監控警示無法按頁面分流 |
| 🟡 | `POST /contradiction` | 矛盾偵測 client mock |
| 🟡 | `GET /tracking-feed` | 追蹤通知 tab 空白 |

### 等 R4 — vision-OCR ingestion

| 項目 | 影響 |
|------|------|
| vision-OCR ingestion 完成（法說簡報頁 → `chunks` `doc_type=presentation`）| R7 改 `structured_store.py` 一行 SQL 後 `/library?doc_type=earnings_call` 自動生效 |

> ColPali gate ③ FAIL（2026-06-23），#22 已關。新路線：`docs/superpowers/specs/2026-06-23-vision-ocr-to-text-ingestion-design.md`

### 等 R1/R2

| 項目 | 影響 |
|------|------|
| Cloud Run 補 `GOOGLE_CLIENT_ID` | 訂閱 / 歷史需登入端點 401 |

---

## 四、本次 commit 改動清單

| 類型 | 檔案 |
|------|------|
| **修改** | `src/polaris/structured_store.py`（event_key/source_key + list_library 重設計）|
| **修改** | `src/polaris/api.py`（EventResponse + /library 端點）|
| **修改** | `frontend/src/types/viewmodel.ts`（SummaryItemVM doc_type_label）|
| **修改** | `frontend/src/lib/adapters.ts`（normalizeResearch doc_type_label）|
| **修改** | `frontend/src/hooks/useFinancials.ts`（financialsToKpis 補 cite）|
| **修改** | `frontend/src/app/(dashboard)/research/page.tsx`（量化分析隱藏 + chip 修正 + KPI cite）|
| **修改** | `frontend/src/app/(dashboard)/peer/page.tsx`（dynamic fiscal_period）|
| **修改** | `frontend/src/types/api.ts`（已追蹤的前期改動）|
| **修改** | `frontend/src/app/(dashboard)/history/page.tsx`（前期改動）|
| **修改** | `frontend/src/app/(dashboard)/news/page.tsx`（前期改動）|
| **修改** | `frontend/src/app/(dashboard)/notifications/page.tsx`（前期改動）|
| **修改** | `frontend/src/app/(dashboard)/settings/page.tsx`（前期改動）|
| **新增** | `docs/r7/工作紀錄_20260624.md`（本文件）|
| **刪除** | 14 個舊 R7 文件（已於 2026-06-23 合併至 `docs/r7/`）|
