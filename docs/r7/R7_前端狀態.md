# R7 前端狀態

> 維護：R7（angelali2026888）
> 最後更新：2026-06-24
> 合併自：`cross-role-collab/R7_前端待辦清單.md`、`R7_等待清單_20260622.md`

---

## 後端串接狀態

> 更新日期：2026-06-18（初版）→ 2026-06-22（狀態回填）
> FastAPI 後端已可本地啟動（`make serve-api`，port 8000）。BigQuery 連線正常。

| 端點 | 狀態 | 備註 |
|------|------|------|
| `POST /research` | ✅ 可用 | 回傳 `final_answer` + `evidence[]`，BQ 有真實法說資料 |
| `GET /companies` | ✅ 可用 | 20 間公司，含中文名 |
| `GET /financials` | ✅ 可用 | 月報 `revenue`/`revenue_yoy`/`ytd_yoy` 等 |
| `GET /events` | ✅ 可用 | `event_key` 欄位對齊（R6 更名），`STARTS_WITH` 支援子類型 |
| `GET /library` | ⚠️ 部分可用 | transcript + major_news ✅；earnings_call 等 R4 vision-OCR ingestion（chunks doc_type=presentation）|
| `GET /alerts` | ✅ 可用 | 5 筆 MOPS Watchdog 警示；但缺 `origin`/`title`/`source`/`time` 欄位 |
| `GET /notifications` | ✅ 可用 | 已讀/未讀標記正常（PR #137） |
| `GET /subscriptions` | ✅ 可用 | R7 實作 Firestore（PR #137） |
| `POST /subscriptions` | ⚠️ 401 | Cloud Run 缺 `GOOGLE_CLIENT_ID`，本地可用，線上 401 |
| `DELETE /history/{id}` | ✅ 可用 | R2 實作（PR #138）；本地 fallback `historyStore.remove()` |
| `GET /suggestions` | ❌ 不存在 | 前端 hook 已有 fallback，靜態 chip 正常 |
| `POST /contradiction` | ❌ 不存在 | 前端走 client-side mock |
| `GET /chunk/{source_id}` | ❌ 不存在 | #115 OPEN；DocViewer 走降級 snippet |
| `POST /peer-compare` | ❌ 不存在 | 整頁 hardcoded mock |

---

## 各頁面完成狀態

### 研究助理頁（/research）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 「送同業比較」按鈕 | 帶 query 跳轉 `/peer?q=...` | ✅ |
| 快速提問 chip（動態） | 串 `GET /suggestions`；404 fallback 靜態 PRESETS | ✅ |
| 完整報告匯出 PDF | `ReportModal` 已實作 | ✅ |
| NFR-031 disclaimer | `ComplianceBanner` 移至結果頂部 | ✅ 2026-06-18 |
| KPI 格接 R4 財務資料 | `useFinancials` hook；`GET /financials` 拉月報 YoY | ✅ 2026-06-18 |
| 量化分析 panel | R4 建議延後，UI 隱藏並留接線位置 | ✅ 2026-06-24 隱藏 |
| 引用 chip 顯示 doc_type 名稱 | `SummaryItemVM.doc_type_label` 從 evidence 取 | ✅ 2026-06-24 |
| 財務 KPI cite 引用 | `financialsToKpis` 補 `source_id` | ✅ 2026-06-24 |
| Loading 動畫 | dock-send 旋轉、三點彈跳、5s 切換提示 | ✅ 2026-06-22 |
| contraAlertStore 頁面隔離 | page-scoped sessionStorage key | ✅ 2026-06-22 |
| 矛盾偵測 → 真實後端 | 等 `POST /contradiction` | ⏳ 等 R3 |
| 引用追蹤器點開原文 | 等 `GET /chunk/{source_id}` | ⏳ 等 R3（#115） |
| 監控警示按頁面過濾 | 等 `GET /alerts` 補 `origin` 欄位 | ⏳ 等 R3 |
| KPI 標籤顯示年月 | 等 R3 部署 `v_financial_metrics_semantic` | ⏳ 等 R3 |

### 同業比較頁（/peer）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 示範資料 banner | `.mock-note` 黃色警示橫幅 | ✅ 2026-06-18 |
| 公司中文名顯示 | `normalizeCompany` 修正 | ✅ 2026-06-18 |
| Loading 動畫 | 與研究助理同步 | ✅ 2026-06-22 |
| contraAlertStore 頁面隔離 | 同研究助理 | ✅ 2026-06-22 |
| 季度選單動態化 | 從 `useFinancials` 取實際可用季度 | ✅ 2026-06-24 |
| 快速提問 chip 動態化 | 等 `GET /suggestions?mode=peer` | ⏳ 等 R3 |
| 整頁換真實資料 | 等 `POST /peer-compare` | ⏳ 等 R3 |
| 矛盾偵測 → 真實後端 | 等 `POST /contradiction` | ⏳ 等 R3 |
| 監控警示按頁面過濾 | 同研究助理 | ⏳ 等 R3 |

### 通知中心（/notifications）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 訂閱設定 UI | 搜尋框 + 下拉選單 + chip 清單 | ✅ 2026-06-22 |
| 深色模式下拉顏色 | 改用 `rgb(var(--card))`/`--border`/`--foreground` | ✅ 2026-06-22 |
| 矛盾警示過濾 | `level: "info"` 不進通知中心 | ✅ 2026-06-22 |
| 追蹤通知顯示 | 等 `GET /tracking-feed` | ⏳ 等 R3 |

### 新聞頁（/news）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 串接 `events` 資料表 | `GET /events?type=news` | ⚠️ 後端 500（待 R3 修） |

### 資料庫頁（/library）

| 項目 | 說明 | 狀態 |
|------|------|------|
| transcript + major_news | R7 實作 `GET /library`（structured_store.py）| ✅ 2026-06-24 |
| earnings_call（法說簡報）| 等 R4 vision-OCR ingestion（chunks `doc_type=presentation`）| ⏳ #22 已關，R7 改 SQL 後自動生效 |

### 對話紀錄頁（/history）

| 項目 | 說明 | 狀態 |
|------|------|------|
| Firestore B 級還原 | 登入後整包 result 存 Firestore | ✅ PR #137 |
| 刪除確認卡片 | 兩步確認 + loading 狀態 | ✅ 2026-06-22 |
| DELETE fallback | 後端 404 時走 `historyStore.remove()` | ✅ 2026-06-22 |

### 設定頁（/settings）

| 項目 | 說明 | 狀態 |
|------|------|------|
| Google OAuth 串接 | NextAuth.js + `signIn("google")` | ✅（等 Cloud Run `GOOGLE_CLIENT_ID`） |
| Magic Link | — | ✅ 砍（2026-06-18 決策） |

---

## 等待清單（2026-06-22）

> 前端無主動待辦，以下全部等待他人交付後再接手。

### 🔴 等 R3 — 高優先（阻擋功能）

| 優先 | 端點 | R7 接手動作 |
|------|------|------------|
| 🔴 P0 | `GET /events` 500 修復 | 自動生效，新聞頁出現 |
| 🔴 P0 | 部署 `structured_store.py` + `api.py`（year/month） | KPI 標籤自動改為年月格式 |
| 🔴 P1 | `GET /chunk/{source_id}` | `handleOpenDoc` 改呼叫真實端點 |
| 🔴 P1 | `GET /alerts` 補 `origin`/`title`/`source`/`time` | `research/page.tsx` + `peer/page.tsx` 各加 filter |
| 🔴 P1 | `POST /contradiction` | 移除 client mock，改呼叫真實端點 |

### 🟡 等 R3 — 中優先

| 優先 | 端點 | R7 接手動作 |
|------|------|------------|
| 🟡 P2 | `POST /research` 補 `kpis`/`chart` | `normalizeResearch()` 解封兩行即可 |
| 🟡 P2 | `GET /tracking-feed` | 通知中心追蹤 tab 接真實資料 |
| 🟡 P2 | `GET /suggestions?mode=peer` | 同業比較 chip 動態化 |
| 🟡 P2 | PR #10 merge | `adapters.ts` citation 欄位自動生效 |

### 🟡 等 R4

| 端點 | R7 接手動作 |
|------|------------|
| `GET /library` | 前端 UI 已就緒，端點上線後自動顯示 |

### 🔴 等 R1/R2 — 部署

| 項目 | 影響 |
|------|------|
| Cloud Run 補 `GOOGLE_CLIENT_ID` | 訂閱 / 歷史所有需登入端點 401 |

---

## 已修正的隱藏 Bug（歷史記錄）

| 日期 | Bug | 修正 |
|------|-----|------|
| 2026-06-18 | `normalizeCompany` 欄位錯誤，公司名全是 undefined | `raw.id→raw.ticker`、`raw.name→raw.company_name??raw.ticker` |
| 2026-06-18 | `normalizeAlert` 欄位錯誤，alerts 全欄位 undefined | 對齊後端 `{event_id,ticker,severity,evidence[]}` 結構 |
| 2026-06-18 | `normalizeNotifications` unread 讀 `raw.unread`，後端實際回 `unread_count` | 改 `raw.unread_count` |
| 2026-06-22 | contraAlertStore 跨頁污染 | 改 page-scoped sessionStorage key |
| 2026-06-22 | notifications 頁 `useContraAlerts()` TS2554 build error | 改 `useContraAlerts("research")` + `useContraAlerts("peer")` |
