# R7 開發日誌

> 維護：R7（angelali2026888）
> 合併自：`R7_本次改動摘要_2026-06-19.md`、`工作紀錄_20260622.md`、`進度紀錄_20260622.md`

---

## 2026-06-19 — PR 改動摘要

> Branch: `frontend-UI_2026_0618`
> Commit: `9c7d882` (fix) / `1917ae7` (feat, 主要批次)

### Issue #118 — /ask adapter 對齊後端契約 + NFR-031 surface

**`frontend/src/types/api.ts`**
- 新增 `AskCitationRaw`、`NodeTraceRaw` interface（對齊 live 後端欄位）
- 改寫 `AskResponse`：`{ answer, compliance_status, citations: AskCitationRaw[], trace: NodeTraceRaw[] }`
- 新增 `ChunkRaw` interface（對應 GET /chunk/{source_id} 契約）

**`frontend/src/types/viewmodel.ts`**
- `AskVM` 加入 `compliance_status: string` 欄位

**`frontend/src/lib/adapters.ts`**
- `normalizeAsk(raw, query)` 重寫：從 `raw.answer` 解析 bullets、從 `raw.citations` 建 summary、`raw.trace` 轉 ReAct steps
- `normalizeResearch` 補上 `compliance_status: raw.compliance_status`
- 新增 helper：`nodeTracesToReact()`、`askCitationToTracker()`

**`frontend/src/app/(dashboard)/research/page.tsx`**
- `handleOpenDoc` 改為 async：優先呼叫 `api.chunk()` 取後端真實文件，fallback 本地 DOC_CONTENTS，再 fallback VM snippet
- `compliance_status === "blocked"` 條件渲染：blocked 時只顯示 `<ComplianceBanner>`、抑制 kpis/summary/chart 輸出（NFR-031）
- `data?.chart` 全面改為 `displayData?.chart`

### Issue #114 — Google 登入 + /history B 級 + Magic Link 移除

**新增檔案**
- `frontend/src/app/api/auth/[...nextauth]/route.ts`：NextAuth v4 + Google Provider；JWT callback 存 `id_token`
- `frontend/next.config.ts`：`output: "standalone"`（Cloud Run 必需）
- `frontend/Dockerfile`：三階段 build；`$PORT` 注入監聽埠

**修改檔案**
- `frontend/src/lib/api.ts`：新增 `authHeaders()`；`api.history()` 登入/未登入 fallback；新增 `api.chunk()`
- `frontend/src/app/(dashboard)/settings/page.tsx`：移除 Magic Link，Google 按鈕接 `signIn("google")`
- `frontend/src/components/layout/AppShell.tsx`：顯示真實 `name`、`image`

### 等待事項（當時）

| 項目 | 負責人 |
|------|--------|
| `GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`、`NEXTAUTH_SECRET` | R2/PM |
| 部署 `polaris-web` 到 Cloud Run | R2/PM |
| `GET /chunk/{source_id}` 後端實作 | R3 |

---

## 2026-06-22 — 工作紀錄

### 上午：安全強化

- ✅ 建立 `.claude/hooks/block-env-read.sh`：攔截 Read/Bash/PowerShell/Grep 讀取 .env
- ✅ `redaction.py` 補 `TAVILY_API_KEY` pattern
- ✅ `.gitignore` 補 `frontend/.next/dev/logs/`
- ✅ 刪除 worktree 殘留 `.env.local`

### 上午：Git / Remote 切換

- ✅ remote origin 切換至 `holajennytw/polaris-desk`
- ✅ Merge `origin/main`（`5d8aad7`）無衝突

### 下午：PR #4 review 回應 + 接口修復

- ✅ `block-env-read.sh` 完整重寫（sed/grep，修 Windows stdin pipe 問題）
- ✅ 加 `.gitattributes`：`*.sh eol=lf`
- ✅ `notifications/page.tsx`：`toggleSub` catch 補 `saveError` state

### 下午：新手引導完整離線化

- ✅ `research/page.tsx`：加 `TOUR_MOCK_RESULT`（台積電 2026Q1 假資料）+ 假 2s 進度動畫
- ✅ `OnboardingModal.tsx`：`signIn` 補 `callbackUrl: "/research"`

### 傍晚：UX 優化 + Bug 修復（Loading 動畫）

- ✅ `dock-send` 按鈕：running 時圖示旋轉（1.5s/圈）
- ✅ 模型思考追蹤面板：三點彈跳動畫（`thinking-pulse`/`thinking-dots`）
- ✅ 5 秒閾值：running 超過 5 秒切換提示文字
- ✅ 同業比較頁同步套用

### 傍晚：contraAlertStore 跨頁污染修復

- ✅ **根因**：單一 `sessionStorage` key 導致研究助理 / 同業比較互相污染
- ✅ **修法**：page-scoped key（`polaris_contra_alerts_research` / `polaris_contra_alerts_peer`）
- ✅ `contraAlertStore.ts`、`useContraAlerts.ts`、`research/page.tsx`、`peer/page.tsx`、`notifications/page.tsx` 全部更新

### 晚間：Bug 修復

- ✅ `settings/page.tsx` signIn 補 `callbackUrl: "/settings"`
- ✅ `history/page.tsx` DELETE fallback：後端 404 時改呼叫 `historyStore.remove()` 本地刪除
- ✅ `history/page.tsx` 刪除按鈕加 `isDeleting` loading 狀態

### 晚間：鍵盤操作

- ✅ `research/page.tsx` + `peer/page.tsx`：輸入框 `Enter` 送出（Shift+Enter 不觸發）

---

## 2026-06-22 — 全組進度快照

### Issue 狀態總覽

| Issue | 標題 | 狀態 |
|-------|------|------|
| #3 | R3 待交端點 × 7 | 🔴 OPEN，前端已就緒 |
| #5 | block-env-read.sh fails open | 🟠 PR #9 OPEN |
| #6 | /ask bm25 stub | 🟠 PR #10 OPEN |

### PR #4 Test Plan 驗證

| 測試項目 | 狀態 | 說明 |
|---------|------|------|
| `POST /research` BQ 資料 + evidence[] | ⚠️ | PR #10 (OPEN) 正在修 citation metadata |
| `GET /notifications` 已讀/未讀切換 | ✅ | 正常 |
| `GET/POST /subscriptions` Firestore | ❌ | Cloud Run 缺 `GOOGLE_CLIENT_ID` → 401 |
| `DELETE /history/{id}` + fallback | ❌ | 後端無此路由（PR #138 待 merge） |
| 監控警示面板 5 筆 Watchdog | ⚠️ | AlertResponse 缺 origin/title/source/time |
| 同業比較示範資料 banner | ✅ | 正常 |

### 重要發現：POST /subscriptions 失敗根因

**根因**：Cloud Run 後端缺少 `GOOGLE_CLIENT_ID` 環境變數。
- `src/polaris/auth.py:verify_token()`：`audience=""` → 任何真實 token 的 `aud` 不符 → HTTP 401

**R1/R2 執行**：
```bash
gcloud run services update polaris-api \
  --region asia-east1 \
  --set-env-vars GOOGLE_CLIENT_ID=14326813937-rkr1h4u1akpllkk62l8ui23sqqiau734.apps.googleusercontent.com
```

### 各角色待辦（當時快照）

| 項目 | Owner |
|------|-------|
| Cloud Run 補 `GOOGLE_CLIENT_ID` | R1/R2 |
| `GET /chunk/{source_id}` 上線 | R3（#115） |
| `GET /alerts` 補 `origin` 欄位 | R3 |
| Eval ≥ 80% | R5 |
| ColPali query encoder | R4（#133） |

### R3 向量路修復方向（2026-06-22 確認）

- **P1 不能直接切 `v_chunk_semantic`**：該 view 不含 `embedding` 欄，BigQuery VECTOR_SEARCH 需要真實 embedding 欄
- **R3 建議方向**：保留 `chunks` 做向量搜尋，再用 `chunk_id` LEFT JOIN `v_chunk_semantic` 取 `event_key`/`source_key`/`published_yyyymm`
- **R7 影響**：不需改程式碼；PR #10 合進後自動生效

### 本地環境狀態（2026-06-22 傍晚）

- 分支：`frontend_2026_0622`
- dev server：`localhost:3000` ✅
- TypeScript：✅ 無錯誤
- `GOOGLE_CLIENT_SECRET`：已由 Jenny(R4) 補入 `.env.local` ✅
