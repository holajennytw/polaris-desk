# 工作紀錄 — 2026-06-23（ColPali 第 4 路）

## 今日完成事項

### ColPali 第 4 路（#133 / TD-02）
- ✅ **#133 query encoder 併入 `main`**（merge `226c814`，branch `r4/colpali-v12-query-encoder`）：
  `src/polaris/retrieval/colpali_query_encoder.py` = colpali-v1.2，query token → **mean-pool** → 128 維。
  `active_colpali_query_fn()` 在 `COLPALI_QUERY_ENCODER=1` 時才建 encoder；**預設關閉** →
  回 None、第 4 路關閉、CI 0-import / 0-下載（torch + colpali-engine 延遲載入，新增 `.[colpali]` extra）。
  全測試 731 passed。
- ✅ **TD-02 PM 簽核**（merge `eb6e29a`）：WayneSHC 核可「復原為 gated 第 4 路」。記於
  `G3_readiness.md`、`架構_檢索路徑與ask流程.md`(P2)、`retriever.py`。**僅核可復原，未啟用 prod。**
- ✅ **round-trip 驗收腳本備好**：`scripts/colpali_roundtrip_check.py` 補真實候選 page_id + GPU runbook。

### 修正 / 回退
- ⚠️ 我先前的 `/ask` 語意 enrichment（branch `claude/clever-rhodes-cc1234`）**與 upstream 重複且衝突**
  —— `jenny/main` 早已做（PR #13 r3-ask-citation-fields + v_chunk_semantic JOIN，且更完整）。**已丟棄，未併入。**
  教訓：origin(WayneSHC) 落後 jenny/main 10 commits，**動工前先 `git fetch jenny && git log main..jenny/main`**。
- ⚠️ 我曾把該 stale 版本部署成 **rev 00015**（漏掉 upstream 10 個 commit，屬退步）→ **已回退，prod 100% 回 rev 00014**（團隊 6/22 build）。

---

## 待跟進事項（接下來要做什麼）

> 第 4 路 prod 啟用 = **3 閘全綠**才設 `COLPALI_QUERY_ENCODER=1` 上 prod：
> ① #133 encoder ✅ ② TD-02 PM 簽核 ✅ ③ **≥70% round-trip ⏳（唯一未過）**

| # | 下一步 | Owner | 阻擋 / 備註 |
|---|--------|-------|------------|
| 1 | **跑 ≥70% round-trip 驗收**（閘 ③）：在 **GPU 環境**（Colab 免費 GPU 或 R4 GCE）`export HF_TOKEN=...`（colpali-v1.2 依賴 *gated* `google/paligemma-3b-mix-448`）→ `uv pip install -e '.[colpali]'` → `COLPALI_QUERY_ENCODER=1 uv run python scripts/colpali_roundtrip_check.py` | **R4（Jenny）** | 本機 Mac 無 GPU、無 HF token，跑不了 |
| 2 | **填 GOLD 樣本**：腳本 `_CANDIDATE_PAGES` 已附 5 個真實 page_id（statelevel：統一/鴻海/台積電/聯發科/中信金）；R4 開該頁看圖表內容、寫成自然語 query、搬進 `GOLD` | **R4（Jenny）** | 缺頁面圖內容無法代填；挑辨識度高的圖表頁（毛利趨勢/營收分區）命中率才有意義 |
| 3 | **確認池化對齊**：本 encoder 對 query 做 **mean-pool**；R4 須確認 `colpali_pages` 入庫時 page 端也是 mean-pool over patches，否則不同空間、命中率失真 | **R4（Jenny）** | 決定閘 ③ 是否可信 |
| 4 | **閘 ③ 結果分流**：≥70% → PM 設 `COLPALI_QUERY_ENCODER=1`（Cloud Run env）+ 從 jenny/main 重新 deploy 上 prod；<70% → 依 TD-01 再砍場景 3 + ColPali | **PM（Wayne）** | 等 #1 結果 |
| 5 | **清理**：刪本機 stale branch `claude/clever-rhodes-cc1234`（重複的 store 改動，未推送、無價值） | 任一 | 純清理 |

### `/ask` 語意引用（#100）— 無需新動作
`jenny/main` 已上 event_key/source_key/published_yyyymm（PR #13）。prod rev 00014 為團隊現役版本，未受我影響。
若要把這些欄位實際部到 prod，照常規從 jenny/main deploy 即可（非本次範圍）。

> GitHub 通知：WayneSHC 帳號 6/22 起停權中，無法 @mention/開 issue。本文committed 進 repo 當通知；
> 帳號恢復後再於 GitHub @R4 催 #1–#3。
